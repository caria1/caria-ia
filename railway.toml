[build]
builder = "dockerfile"
dockerFilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn main:app --proxy-headers --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
